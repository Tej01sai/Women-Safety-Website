{
  "name": "women-safety-frontend",
  "version": "1.0.0",
  "description": "Guardian Shield Frontend",
  "main": "index.html",
  "scripts": {
    "start": "live-server --port=3000 --host=localhost",
    "dev": "live-server --port=3000 --host=localhost --watch=."
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
